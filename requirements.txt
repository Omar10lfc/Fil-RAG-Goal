requests==2.31.0
beautifulsoup4==4.12.3
lxml==5.2.2
sentence-transformers==2.7.0
faiss-cpu==1.8.0
torch==2.2.0
groq==0.9.0
httpx<0.28  # groq 0.9.0 uses the old `proxies` kwarg removed in httpx 0.28+
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.1
numpy==1.26.4
python-dotenv==1.0.1
rank-bm25==0.2.2
slowapi==0.1.9
# NOTE: gradio is NOT a core dependency (only the HF Space launcher app.py and
# the legacy frontend/app.py Gradio UI need it). Space/mobile installs use
# requirements-space.txt instead.
